#include "Camera.h"
#include <glad/glad.h>

namespace ReGL
{
    bool Camera::Render() const
    {
        if(this == nullptr)
        {
            return true;
        }
        return true;
    }


    bool Camera::Clear() const
    {
        
        return true;
    }
}
